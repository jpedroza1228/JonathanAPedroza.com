# name: Notify Profile Repo
# on:
#   push:
#     branches:
#       - main # Or your default branch

# jobs:
#   ping-profile:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Emit repository dispatch
#         run: |
#           curl -X POST https://api.github.com/repos/YOUR_USERNAME/YOUR_PROFILE_REPO_NAME/dispatches \
#           -H "Accept: application/vnd.github.v3+json" \
#           -H "Authorization: Bearer ${{ secrets.PROFILE_REPO_TOKEN }}" \
#           -d '{"event_type": "website_updated"}'